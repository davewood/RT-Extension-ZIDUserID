<%ARGS>
$User
</%ARGS>
<%INIT>
my $name     = $User->Name;
my $uaccount = '';
if (
    $name !~ m/^(RT_System|Nobody)$/xms
    && $name =~ m/^[a-zA-Z0-9]+$/xmsi
    && $User->Privileged
) {
  my $url = "https://www.univie.ac.at/ZID/uaccount/admin/index.html?action=detail&username=$name";
  $uaccount = qq!<a href="$url">$name (uaccount)</a> - !;
}
</%INIT>
<% $uaccount | n %>\
